export default function Products({products , baseUrl}) {
    return (
        <section className="flex flex-col items-center " id={`error${crypto.randomUUID().slice(-8)}`}>
            <div className="w-full max-w-7xl px-14 my-10 flex flex-col justify-between gap-16">
                <h2>Ops, Página não encontrada!</h2>
            </div>
        </section>
    )
}